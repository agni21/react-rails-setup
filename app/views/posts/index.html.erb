<p id="notice"><%= notice %></p>

<h1>Posts</h1>

<%= link_to 'New Post', new_post_path %>

</table>
<div class="container">
  <% @posts.each do |post| %>
    <div class="row">
      <div class="col-md-12">
        <div class=""><h2><%= post.title %></h2></div>
      </div>
      <div class="col-md-8">
        <div class=""><p><%= post.content %></p></div>
      </div>
      <div class="col-md-3">
        <div class=""><%= post.user.email %></div>
        <div class=""><%= link_to 'Show', post %></div>
        <div class=""><%= link_to 'Edit', edit_post_path(post) %></div>
        <div class=""><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></div>
      </div>
      <hr>
    </div>
  <% end %>
</div>

<br>
